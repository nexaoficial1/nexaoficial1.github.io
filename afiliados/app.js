async function load(path){const r=await fetch(path);if(!r.ok)throw new Error(path);return r.text()}async function start(){const app=document.getElementById('app');try{app.insertAdjacentHTML('beforeend',await load('hero.html'));app.insertAdjacentHTML('beforeend',await load('products-head.html'));const cards=document.getElementById('productCards');for(const file of ['product-3321.html','product-novo-afiliado.html','product-reconexao.html'])cards.insertAdjacentHTML('beforeend',await load(file));for(const file of ['trust.html','flow.html','faq.html'])app.insertAdjacentHTML('beforeend',await load(file))}catch(e){app.innerHTML='<section class="section"><h2>Central NEXA</h2><p class="intro">Não foi possível carregar esta página agora. Tente atualizar.</p></section>'}}start();